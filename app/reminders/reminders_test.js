'use strict';

describe('ygAdmin.reminders module', function() {

  beforeEach(module('ygAdmin.reminders'));

  describe('reminders controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var remindersCtrl = $controller('RemindersCtrl');
      expect(remindersCtrl).toBeDefined();
    }));

  });
});