'use strict';

describe('ygAdmin.categories module', function() {

  beforeEach(module('ygAdmin.categories'));

  describe('categories controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var categoriesCtrl = $controller('CategoriesCtrl');
      expect(categoriesCtrl).toBeDefined();
    }));

  });
});